import React, {Component} from 'react';

import Card, { CardActions, CardHeader} from 'material-ui/Card';

// var moment = require("moment");

// import Articles from '../../articles/index';

import Article from '../../../articles/article';


export default class TopicView extends Component {


  render(){

    return (
      <Article
        display={true}
        document={this.props.document && this.props.document.document}
        comments={this.props.document && this.props.document.document && this.props.document.document.comments}
        user={this.props.user && this.props.user.user}
        documentActions={this.props.documentActions}
      />
    );
  }
}

