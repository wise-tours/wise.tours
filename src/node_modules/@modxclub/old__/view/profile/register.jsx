import React, {Component} from 'react';
import ReactDOM from 'react-dom';

import TextField from 'material-ui/TextField';
import Card, { CardActions, CardHeader, CardMedia } from 'material-ui/Card';
import Button from 'material-ui/Button';

const defaultProps = {
};


export default class RegisterView extends Component {

  onHandleRegister(){

  }

  onHandleForgotPassword(){

  }

  render(){

    return (
      <Card>
        <CardHeader>
          <h2>Регистрация</h2>
        </CardHeader>
          <div className="row">
            <div className="col-md-6">
              <TextField
                label="Логин"
                fullWidth={true}
                ref="username"
              />
              <TextField
                label="ФИО"
                fullWidth={true}
                ref="fullname"
              />
              <TextField
                label="E-mail"
                fullWidth={true}
                ref="email"
              />
              <p>
                <Button
                  onTouchTap={this.onHandleRegister.bind(this)}
                >
                  Регистрация
                </Button>
              </p>
              <p>Если вы были зарегистрированы ранее, можете попробовать <a onTouchTap={this.onHandleForgotPassword.bind(this)}>восстановить пароль</a></p>
            </div>
          </div>
      </Card>
    );

    return null;
  }


}


RegisterView.defaultProps = defaultProps;
