import React from 'react';

import Pagination from '../../pagination';

// import {DataStore, Dispatcher} from 'react-cms-data-view';
import Table, { TableBody, TableCell, TableHead, TableRow } from 'material-ui/Table';
import Paper from 'material-ui/Paper';

import {Link} from 'react-router-dom';

import SuccessIcon from 'material-ui-icons/Check';
import FailureIcon from 'material-ui-icons/Clear';

import IndexView from '../index/';
import CompaniesList from './list';

export default class CompaniesView extends IndexView {

  constructor(props){

    super(props);

    Object.assign(this.state, {
    	pagetitle: "Каталог веб-студий",
    	pageHeader: "Каталог веб-студий",
    });
  } 


  // componentDidMount(){

    
  //   this.setState({
  //   	pagetitle: "DSfdsf",
  //   });

  //   return super.componentDidMount && super.componentDidMount();
  // }

  contentBlock(){

    let {
      user,
    } = this.props;

  	let {
  		pagetitle,
  		data,
  	} = this.props.document.document || {};

  	let {
  		object: companies,
  		page,
  		total,
  		limit,
  	} = data || {}

    return <CompaniesList 
      companies={companies || []}
      page={page}
      total={total}
      limit={limit}
      user={user}
    />  	
  }
}
